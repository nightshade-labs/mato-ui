/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit'

export const EXITS_DISCRIMINATOR = new Uint8Array([
  240, 175, 85, 167, 2, 200, 2, 180,
])

export function getExitsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(EXITS_DISCRIMINATOR)
}

export type Exits = {
  discriminator: ReadonlyUint8Array
  /** Address which created this account, used to determine where the rent goes if account is closed */
  owner: Address
  /** Stores the base flow that is removed from the market at a given slot because an order ended */
  baseExits: Array<bigint>
  /** Stores the quote flow that is removed from the market at a given slot because an order ended */
  quoteExits: Array<bigint>
  index: bigint
  bump: number
}

export type ExitsArgs = {
  /** Address which created this account, used to determine where the rent goes if account is closed */
  owner: Address
  /** Stores the base flow that is removed from the market at a given slot because an order ended */
  baseExits: Array<number | bigint>
  /** Stores the quote flow that is removed from the market at a given slot because an order ended */
  quoteExits: Array<number | bigint>
  index: number | bigint
  bump: number
}

/** Gets the encoder for {@link ExitsArgs} account data. */
export function getExitsEncoder(): FixedSizeEncoder<ExitsArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['owner', getAddressEncoder()],
      ['baseExits', getArrayEncoder(getU128Encoder(), { size: 20 })],
      ['quoteExits', getArrayEncoder(getU128Encoder(), { size: 20 })],
      ['index', getU64Encoder()],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: EXITS_DISCRIMINATOR }),
  )
}

/** Gets the decoder for {@link Exits} account data. */
export function getExitsDecoder(): FixedSizeDecoder<Exits> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['owner', getAddressDecoder()],
    ['baseExits', getArrayDecoder(getU128Decoder(), { size: 20 })],
    ['quoteExits', getArrayDecoder(getU128Decoder(), { size: 20 })],
    ['index', getU64Decoder()],
    ['bump', getU8Decoder()],
  ])
}

/** Gets the codec for {@link Exits} account data. */
export function getExitsCodec(): FixedSizeCodec<ExitsArgs, Exits> {
  return combineCodec(getExitsEncoder(), getExitsDecoder())
}

export function decodeExits<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Exits, TAddress>
export function decodeExits<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Exits, TAddress>
export function decodeExits<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Exits, TAddress> | MaybeAccount<Exits, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getExitsDecoder(),
  )
}

export async function fetchExits<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Exits, TAddress>> {
  const maybeAccount = await fetchMaybeExits(rpc, address, config)
  assertAccountExists(maybeAccount)
  return maybeAccount
}

export async function fetchMaybeExits<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Exits, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config)
  return decodeExits(maybeAccount)
}

export async function fetchAllExits(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Exits>[]> {
  const maybeAccounts = await fetchAllMaybeExits(rpc, addresses, config)
  assertAccountsExist(maybeAccounts)
  return maybeAccounts
}

export async function fetchAllMaybeExits(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Exits>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config)
  return maybeAccounts.map((maybeAccount) => decodeExits(maybeAccount))
}

export function getExitsSize(): number {
  return 689
}
